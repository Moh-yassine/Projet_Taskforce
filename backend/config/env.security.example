# Configuration de sécurité - Exemple
# Copiez ce fichier vers .env.local et ajustez les valeurs pour votre environnement

# === SÉCURITÉ ===
# Clé secrète pour l'application (générez une nouvelle clé pour la production)
APP_SECRET=your_super_secret_key_here_minimum_32_characters

# === CORS ===
# Origines autorisées pour CORS (séparées par des virgules)
CORS_ALLOW_ORIGIN=https://localhost:5173,https://localhost:5174,https://localhost:5175

# === BASE DE DONNÉES ===
DATABASE_URL="postgresql://app:!ChangeMe!@127.0.0.1:5432/app?serverVersion=16&charset=utf8"

# === STRIPE ===
# ⚠️ IMPORTANT: Ne jamais commiter les vraies clés Stripe !
# Utilisez des variables d'environnement ou un gestionnaire de secrets
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_PUBLIC_KEY=pk_test_your_stripe_public_key_here

# === JWT ===
JWT_SECRET_KEY=%kernel.project_dir%/config/jwt/private.pem
JWT_PUBLIC_KEY=%kernel.project_dir%/config/jwt/public.pem
JWT_PASSPHRASE=your_jwt_passphrase_here

# === ENVIRONNEMENT ===
APP_ENV=prod
APP_DEBUG=false

# === SÉCURITÉ HTTPS ===
# Force HTTPS en production
TRUSTED_PROXIES=127.0.0.1,REMOTE_ADDR
TRUSTED_HOSTS='^(localhost|example\.com)$'

# === LOGS ===
LOG_LEVEL=warning

# === CACHE ===
# Configuration du cache pour la production
CACHE_ADAPTER=redis
CACHE_DSN=redis://localhost:6379
